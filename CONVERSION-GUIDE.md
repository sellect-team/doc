# PPTX 변환 가이드 & 문서 품질 기준

> 이 문서는 세 가지를 기록한다.
> 1. **PPTX → 포털 HTML 변환 방법** (실행 절차와 판단 근거)
> 2. **원본 PPT가 만들어진 방식** (세일링스톤 회사소개서의 디자인 시스템 실측 분석)
> 3. **동급 품질의 문서를 새로 만들기 위한 지침** (HTML 네이티브 작성 시)
>
> 규격 자체는 [AUTHORING.md](AUTHORING.md), 클로드에 붙여넣는 프롬프트는 [PROMPT.md](PROMPT.md)에 있다.
> 이 문서는 그 둘의 배경이 되는 "왜"와 "어느 수준까지"를 담는다.

---

# 1부. PPTX 변환

## 1.1 문서를 올리는 두 가지 경로

| | 경로 A — PPTX 변환 | 경로 B — HTML 네이티브 작성 |
|---|---|---|
| 언제 | 이미 완성된 PPT가 있을 때 | 새 문서를 처음부터 만들 때 |
| 방법 | `scripts/convert-pptx.ps1` | [PROMPT.md](PROMPT.md)로 클로드에 요청 |
| 재현도 | **원본과 100% 동일** (PowerPoint 자체 렌더링) | 원본 개념 없음 |
| 텍스트 선택 | 웹 뷰어에서는 불가, PDF에서는 가능 | 웹·PDF 모두 가능 |
| 수정 | 원본 PPT를 고쳐서 재변환 | HTML을 직접 수정 |
| 용량 | 슬라이드당 100~450KB | 수십 KB |

**판단 기준**: 완성된 PPT가 이미 있으면 무조건 경로 A다. HTML로 재현하려 들면 폰트·간격·아이콘이 미세하게 어긋나고, 원본을 고칠 때마다 두 곳을 관리해야 한다.

## 1.2 왜 이미지 방식인가

변환 시 세 가지 방법을 검토했고 이미지 방식을 택했다.

| 방법 | 재현도 | 문제 |
|---|---|---|
| HTML/CSS 재구성 | 60~80% | 폰트(SUIT) 미설치 환경에서 레이아웃 붕괴, 아이콘 27개/슬라이드 재현 불가, 작업량 과다 |
| SVG 변환 | 90% | 텍스트가 path로 변환되며 용량 폭증, 그라데이션·글로우 효과 손실 |
| **PNG→WebP 이미지** | **100%** | 텍스트 선택 불가 (→ PDF로 보완) |

**PowerPoint COM으로 내보내기 때문에 100%가 가능하다.** LibreOffice나 서드파티 변환기는 폰트 치환과 도형 근사가 일어나지만, PowerPoint 자신이 렌더링하면 화면에 보이는 것과 완전히 같다.

텍스트 선택 문제는 **원본 PDF를 함께 배포**해서 보완한다. 포털의 [PDF 다운로드] 버튼은 이 PDF를 내려주므로, 영업이 검색·복사해야 할 때는 PDF를 쓰면 된다.

## 1.3 실행 절차

### 준비물
- PowerPoint 설치 (COM 자동화용)
- Python + Pillow (`py -m pip install Pillow`)

### 1단계 — 페이지 제목 초안 만들기

```bash
powershell -File "scripts/convert-pptx.ps1" -Source "C:\경로\원본.pptx" -Category company -Name company-intro-2026 -TitlesOnly
```

`docs/company/company-intro-2026.titles.txt`가 생성된다. 각 줄이 슬라이드 1장의 제목이고, 뷰어 왼쪽 목차에 그대로 표시된다.

### 2단계 — 제목 다듬기 (중요)

자동 추출은 슬라이드의 첫 텍스트를 가져오므로 손을 봐야 한다. 실제 사례:

| 슬라이드 | 자동 추출 | 다듬은 결과 | 이유 |
|---|---|---|---|
| 1 | `Corporate Introduction` | `표지 — 비즈니스 바다로의 항해` | 영문 라벨보다 내용이 드러나게 |
| 3 | `01.` | `01. 회사 소개` | 숫자만으로는 목차에서 구분 불가 |
| 10 | `세일링스톤` | `AI 혁신 파트너로서의 강점` | 회사명은 모든 슬라이드에 나와 무의미 |
| 13 | `AiSight` | `AiSight 아키텍처` | 14·15번과 구분되어야 함 |
| 21 | `현장이 '말로 묻고' 검증된 답을 얻는다` | `사례 1 · 현장이 말로 묻는다` | 목차용으로 길이 축약, 번호로 계열 표시 |

**원칙**: 목차만 보고 원하는 페이지를 찾을 수 있어야 한다. 15자 내외, 계열 슬라이드는 `사례 1 ·` 처럼 번호를 앞에 붙인다.

### 3단계 — 변환 실행

```bash
powershell -File "scripts/convert-pptx.ps1" -Source "C:\경로\원본.pptx" -Category company -Name company-intro-2026 -DocTitle "세일링스톤 회사소개서" -Description "회사 개요와 성장 지표, AiSight 솔루션 제품군, 활용 사례까지 담은 표준 회사소개서 (27p)" -Version "1.0"
```

### 4단계 — 빌드 & 확인

```bash
node scripts/build.mjs
```

검증(규격·이미지 존재)을 통과하면 문서 목록에 나타난다. 로컬 확인: `npx http-server site -p 8787 -c-1`

## 1.4 산출물 구조

```
docs/company/
├── company-intro-2026.html           생성된 문서 (27개 section.slide)
├── company-intro-2026.pdf            PowerPoint가 만든 원본 PDF (텍스트 선택 가능)
├── company-intro-2026.titles.txt     페이지 제목 (재변환 시 재사용됨 — 삭제 금지)
└── assets/company-intro-2026/
    └── slide-01.webp … slide-27.webp  2560×1440, WebP 품질 88
```

빌드 시 처리:
- `.webp` → `site/docs/company/assets/…` 로 복사
- `.pdf` → `site/pdf/<문서ID>.pdf` 로 복사하고 **Playwright PDF 생성을 건너뛴다** (원본이 항상 더 정확하므로)
- `.titles.txt` → 사이트로 복사되지 않음

## 1.5 해상도와 용량 기준

| 항목 | 값 | 근거 |
|---|---|---|
| 내보내기 해상도 | 2560×1440 | 27인치 QHD·레티나에서 선명. PDF로 넣어도 192DPI 확보 |
| 변환 포맷 | WebP 품질 88 | PNG 8.5MB → 3.6MB (43%). 텍스트 경계·SQL 코드까지 손상 없음 |
| 슬라이드당 평균 | 150~170KB | 43장 문서 기준 7.2MB — 아래 지연 로딩으로 해결 |

### 문서 전체 용량은 신경 쓰지 않아도 된다

문서가 커도(43장 7.2MB) **초기 로딩은 3장분(약 500KB)뿐**이다. 앞 2장만 즉시 받고 나머지는 `loading="lazy"`로 두며, 뷰어가 이동할 때마다 **앞뒤 슬라이드를 미리 받아둔다**(`preload()`). 실측:

| 상태 | 로드된 이미지 |
|---|---|
| 문서 열자마자 | 43장 중 3장 |
| 20페이지로 점프 후 | 7장 |
| 3페이지 더 이동 후 | 10장 |

따라서 해상도를 낮출 이유가 거의 없다. 화질 우선으로 2560×1440을 유지한다.
꼭 줄여야 하면 `-Width 1920 -Height 1080` 옵션을 쓴다.

## 1.6 수정과 버전업

원본 PPT를 고친 뒤:

1. 같은 명령을 `-Version "1.1"`로 다시 실행 (제목 파일은 재사용됨)
2. `node scripts/build.mjs`
3. 커밋: `[company-intro-2026] v1.1 2026년 실적 슬라이드 추가`

이미지·PDF가 통째로 교체되므로 Git에서 이전 버전 이미지도 보관된다. 뷰어의 "버전 기록"에서 이전 버전을 열면 **그 시점의 슬라이드 이미지가 그대로** 나온다.

## 1.7 함정 (실제로 겪은 것들)

| 증상 | 원인 | 해결 |
|---|---|---|
| `-DocTitle 파라미터를 찾을 수 없음` | PowerShell 5.1이 BOM 없는 UTF-8 .ps1을 ANSI로 읽어 한글이 깨지고 파싱이 실패 | **.ps1 파일은 반드시 UTF-8 BOM으로 저장** |
| `Cannot convert "0" to type "Object"` | `ExportAsFixedFormat`의 COM 인자 타입 변환 실패 | `SaveCopyAs($path, 32)` 사용 |
| `docs/ 폴더가 없습니다` | 경로에 공백(`html ppt`)이 있는데 `import.meta.url`을 직접 파싱 | `fileURLToPath()` 사용 |
| 원본이 OneDrive에 있어 열기 실패 | 동기화 중 파일 잠금 | 스크립트가 임시 폴더로 사본을 만들어 처리 (자동) |

---

# 2부. 원본 PPT 디자인 시스템 분석

세일링스톤 회사소개서(`회사소개서 최신 효성용 v0702 clean.pptx`, 27장)를 실측한 결과다.
**새 문서를 만들 때 이 시스템을 따르면 기존 자료와 톤이 맞는다.**

## 2.1 캔버스

- **720 × 405 pt** (10in × 5.625in), 16:9
- 일반 PPT 기본값(13.33in)의 75% 크기 → 그래서 본문 폰트가 7~11pt로 작다. **작아 보이지만 화면 비율로는 정상**이다.

## 2.2 폰트

| 용도 | 폰트 | 사용 빈도 |
|---|---|---|
| 기본 | **SUIT** | 2041회 |
| 강조·큰 숫자 | **SUIT ExtraBold** | 482회 |
| 보조 설명 | **SUIT Light** | 318회 |
| 중간 강조 | SUIT Medium | 52회 |
| 영문·숫자 | Arial | 607회 |
| 보조 한글 | Noto Sans KR | 236회 |

SUIT는 무료 한글 폰트다. **HTML에서는 Pretendard로 대체한다** — 같은 계열의 기하학적 산세리프로 자막감이 가장 가깝고, CDN으로 웹폰트를 바로 쓸 수 있다.

## 2.3 색상 팔레트 — Tailwind slate + teal

실측 결과 **Tailwind CSS의 slate·teal 스케일을 그대로 쓰고 있다.** 우연이 아니라 의도된 선택으로 보인다.

| 역할 | HEX | Tailwind | 사용 |
|---|---|---|---|
| 최상위 텍스트 / 다크 배경 | `#0F172A` | slate-900 | 156회 |
| 다크 카드 배경 | `#1E293B` | slate-800 | 58회 |
| 본문 텍스트 | `#475569` | slate-600 | 217회 |
| 보조 텍스트 | `#64748B` | slate-500 | 208회 |
| 흐린 텍스트 | `#94A3B8` | slate-400 | 66회 |
| 테두리 | `#CBD5E1` | slate-300 | 63회 |
| 옅은 테두리 | `#E2E8F0` | slate-200 | 41회 |
| 카드 배경 | `#F8FAFC` | slate-50 | 64회 |
| **포인트(주)** | `#0D9488` | **teal-600** | 70회 |
| **포인트(밝음)** | `#2DD4BF` | **teal-400** | 40회 |
| 포인트(어두움) | `#115E59` | teal-800 | 42회 |

**구성비**: slate 계열이 화면의 85% 이상을 차지하고, teal은 강조에만 쓴다. 표지·다크 섹션에서는 teal-400을, 밝은 본문에서는 teal-600을 쓴다.

## 2.4 슬라이드 유형 5가지

원본은 27장을 다섯 가지 틀로 구성한다. 이 리듬이 문서의 완성도를 만든다.

| 유형 | 해당 슬라이드 | 배경 | 특징 |
|---|---|---|---|
| **표지** | 1 | slate-900 다크 + 그리드 패턴 + 글로우 | 대형 카피 2줄(teal 강조 1단어), 태그라인, 로고, 하단 배지 |
| **목차** | 2 | 다크 | 01~04 번호 + 한글/영문 병기 |
| **섹션 표지** | 3, 11, 20, 24 | 다크 | `01.` 대형 숫자 + 섹션명 한글/영문. 내용 없음 |
| **콘텐츠** | 4~10, 12~19, 21~23, 25~26 | 밝음(white/slate-50) | 아래 2.5의 공통 골격 |
| **맺음** | 27 | 다크 | `감사합니다.` 한 줄 |

**밝고 어두움을 번갈아 배치한다** — 섹션 표지가 다크로 들어가면서 콘텐츠 구간이 시각적으로 끊긴다.

## 2.5 콘텐츠 슬라이드 공통 골격

모든 콘텐츠 슬라이드가 동일한 4단 구조를 지킨다.

```
┌─────────────────────────────────────────────────────────┐
│ ■ 슬라이드 제목, 강조어는 teal      01 · Who We Are  │ ← 헤더
├─────────────────────────────────────────────────────────┤
│ 이 슬라이드가 주장하는 바를 2~3줄로. 핵심 구절은 teal 볼드. │ ← 리드문
│                                                          │
│ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐                  │
│ │ 1.   │ │ 2.   │ │ 3.   │ │ 4.   │   ← 본문 (카드 그리드) │
│ └──────┘ └──────┘ └──────┘ └──────┘                  │
│                                    – 7 –   SAILINGSTONE │ ← 푸터
└─────────────────────────────────────────────────────────┘
```

1. **헤더**: 좌측에 teal 사각 불릿(■) + 제목, 우측에 `섹션번호 · 섹션영문명`. 아래에 옅은 구분선.
2. **리드문**: 슬라이드의 결론을 먼저 말한다. 핵심 구절만 teal + 볼드.
3. **본문**: 카드 그리드가 기본. 아래 2.6의 패턴 중 하나.
4. **푸터**: 가운데 `– 7 –` 페이지 번호, 우측 `SAILINGSTONE` 워드마크.

## 2.6 반복 사용되는 본문 패턴 6가지

| 패턴 | 예시 슬라이드 | 구성 |
|---|---|---|
| **번호 매긴 4열 카드** | 5 (하이라이트) | `1. 성장 지표` … `4. 기술 경쟁력` + 대형 수치 + 설명 2줄 |
| **대형 KPI 수치** | 5, 6, 9 | `3.8배` `70%+` `73명` `A-/T4` — ExtraBold, teal, 카드 상단 |
| **타임라인 카드** | 7 (레퍼런스) | 연도별 카드를 계단식으로 배치, 최신 연도만 진한 배경으로 강조 |
| **고객사 배지** | 7 | 회색 pill에 회사명(`삼성전자`) + 옆에 프로젝트명. `[진행]` `[예정]` 상태 표시 |
| **아키텍처 다이어그램** | 13, 15, 19 | 다크 컨테이너 안에 4열 기능 블록, 위아래로 화살표 연결, 아이콘 + 제목 + 설명 |
| **2단 비교** | 6 | `10년+ / 5년` 처럼 대비되는 두 수치를 나란히 |

## 2.7 원본이 잘한 것 (배울 점)

- **결론을 먼저 쓴다.** 리드문이 슬라이드의 주장이고, 카드는 근거다.
- **모든 주장에 숫자가 붙는다.** "성장했다" 대신 `3.8배`, `20억 → 93억`.
- **약점을 정면으로 전환한다.** 슬라이드 6은 "설립 5년"이라는 약점을 "팀워크 10년"으로 재정의한다.
- **한글/영문 병기로 격을 만든다.** `핵심 역량` + `CORE BUSINESS`.
- **아이콘을 남용하지 않는다.** 기능 블록의 제목 옆에만 붙이고, 본문에는 쓰지 않는다.

---

# 3부. 동급 품질의 문서를 새로 만들기

HTML 네이티브로 새 문서를 만들 때(경로 B) 위 시스템을 재현하는 방법이다.

## 3.1 pt → rem 환산 공식

원본 캔버스 높이가 405pt이고, 포털 규격은 `html { font-size: clamp(8px, 1.25vw, 20px) }` + `.slide { height: 100vh }`다.
이 조합에서 **1rem은 항상 슬라이드 높이의 2.222%**이므로 (화면 크기와 무관하게 성립):

> ### **rem = 원본 pt ÷ 9**

| 원본 | rem | 용도 |
|---|---|---|
| 32pt | 3.6rem | 표지 대형 카피 |
| 20pt | 2.2rem | 슬라이드 제목 |
| 16pt | 1.8rem | 대형 KPI 수치 |
| 14pt | 1.55rem | 섹션 헤더 |
| 11pt | 1.2rem | 본문 (가장 많이 쓰임) |
| 10pt | 1.1rem | 카드 설명 |
| 8pt | 0.9rem | 캡션, 배지 |
| 7pt | 0.78rem | 푸터, 각주 |

## 3.2 디자인 토큰 (복사해서 사용)

```css
:root {
  /* slate — 화면의 85%를 담당 */
  --s900: #0F172A;  --s800: #1E293B;  --s600: #475569;  --s500: #64748B;
  --s400: #94A3B8;  --s300: #CBD5E1;  --s200: #E2E8F0;  --s50:  #F8FAFC;
  /* teal — 강조에만 */
  --t800: #115E59;  --t600: #0D9488;  --t400: #2DD4BF;
}
```

- 밝은 슬라이드: 배경 `#fff`, 제목 `--s900`, 본문 `--s600`, 보조 `--s500`, 카드 `--s50`, 테두리 `--s200`, 강조 `--t600`
- 다크 슬라이드: 배경 `--s900`, 제목 `#fff`, 본문 `--s400`, 강조 `--t400`

## 3.3 콘텐츠 슬라이드 골격 (그대로 쓸 수 있는 코드)

```html
<section class="slide" data-title="핵심 역량">
  <header class="s-head">
    <h2><span class="bullet"></span>핵심 역량, <em>주요 사업 영역</em></h2>
    <div class="s-sec">01 · Who We Are</div>
  </header>
  <p class="s-lead">
    고객사의 니즈를 완벽히 반영한 <strong>맞춤형 지능화 분석 시스템</strong>을 제공합니다.
  </p>
  <div class="s-grid">
    <div class="card">
      <div class="card-label">CORE BUSINESS</div>
      <h3>BI 시스템 구축</h3>
      <p>운영 고도화와 표준 템플릿 제공까지 함께합니다.</p>
    </div>
    <!-- 카드 3~4개 -->
  </div>
  <footer class="s-foot"><span>– 8 –</span><span class="mark">SAILINGSTONE</span></footer>
</section>
```

```css
.slide { padding: 2.2rem 2.8rem; display: flex; flex-direction: column; }
.s-head { display: flex; align-items: baseline; justify-content: space-between;
          border-bottom: 1px solid var(--s200); padding-bottom: 0.9rem; }
.s-head h2 { font-size: 2.2rem; font-weight: 700; letter-spacing: -0.02em; color: var(--s900); }
.s-head h2 em { color: var(--t600); font-style: normal; }
.bullet { display: inline-block; width: 0.6rem; height: 0.6rem;
          background: var(--t400); margin-right: 0.8rem; vertical-align: middle; }
.s-sec { font-size: 0.9rem; color: var(--s400); letter-spacing: 0.08em; }
.s-lead { font-size: 1.2rem; line-height: 1.7; color: var(--s600); margin: 1.4rem 0 1.8rem; }
.s-lead strong { color: var(--t600); font-weight: 700; }
.s-grid { flex: 1; display: grid; grid-template-columns: repeat(4, 1fr); gap: 1rem; }
.card { background: var(--s50); border: 1px solid var(--s200);
        border-radius: 0.8rem; padding: 1.4rem 1.2rem; }
.card-label { font-size: 0.78rem; font-weight: 700; color: var(--t600); letter-spacing: 0.08em; }
.card h3 { font-size: 1.35rem; font-weight: 700; color: var(--s900); margin: 0.5rem 0 0.4rem; }
.card p { font-size: 1.05rem; line-height: 1.6; color: var(--s500); }
.s-foot { display: flex; justify-content: space-between; font-size: 0.78rem; color: var(--s400); }
.s-foot .mark { letter-spacing: 0.12em; }
```

## 3.4 문서 전체 구성 규칙

27장 원본의 리듬을 따른다:

```
표지 (다크)
목차 (다크)
├ 01 섹션 표지 (다크)   →  콘텐츠 4~7장 (밝음)
├ 02 섹션 표지 (다크)   →  콘텐츠 4~8장 (밝음)
├ 03 섹션 표지 (다크)   →  콘텐츠 2~3장 (밝음)
└ 04 Appendix (다크)    →  증빙 1~2장 (밝음)
맺음 (다크)
```

- 섹션은 **3~4개**. 5개를 넘으면 목차가 무너진다.
- 한 섹션의 콘텐츠는 **8장 이하**. 넘으면 섹션을 쪼갠다.
- 섹션 표지는 반드시 다크로 — 구간 전환이 눈에 보여야 한다.

## 3.5 내용 작성 규칙

- **슬라이드 제목은 주장으로 쓴다.** `조직 구성` (X) → `유기적 협업 인프라, 조직 및 인력구성` (O)
- **리드문에 결론을 먼저.** 카드는 근거일 뿐이다.
- **모든 주장에 숫자를 붙인다.** 숫자가 없으면 그 주장은 넣지 않는다.
- **카드 설명은 2줄 이내.** 넘치면 카드 수를 줄인다.
- **한글/영문 병기**는 카드 라벨과 섹션명에만. 본문에 쓰면 지저분해진다.

## 3.6 하지 말 것

- 한 슬라이드에 메시지 2개 이상 담기 (→ 슬라이드를 나눈다)
- 카드 5개 이상 한 줄에 배치 (4개가 한계)
- teal을 배경 넓은 면적에 쓰기 (강조가 죽는다)
- 본문 텍스트 가운데 정렬 (제목만 가운데)
- 제목 아래 장식용 밑줄·색 띠 (AI가 만든 문서처럼 보인다)
- `px` 고정 글자 크기 (창 크기가 바뀌면 레이아웃 붕괴 — 검증기가 경고한다)

---

# 4부. 체크리스트

## PPTX 변환 시
- [ ] `-Name`이 영문 소문자-하이픈인가?
- [ ] `.titles.txt`의 제목을 목차용으로 다듬었는가? (15자 내외, 계열은 번호)
- [ ] `-DocTitle`, `-Description`, `-Version`을 채웠는가?
- [ ] 슬라이드 이미지가 전부 생성됐는가? (빌드 검증이 누락 이미지를 잡아준다)
- [ ] `node scripts/build.mjs`가 검증을 통과했는가?
- [ ] 뷰어에서 27장 전부 이미지가 보이고, PDF 다운로드가 원본 PDF인가?

## 새 문서 작성 시
- [ ] 표지 / 목차 / 섹션 표지(다크) / 콘텐츠(밝음) / 맺음 리듬을 지켰는가?
- [ ] 모든 콘텐츠 슬라이드가 헤더-리드문-본문-푸터 4단 골격인가?
- [ ] 색상이 slate 85% + teal 강조 비율인가?
- [ ] 글자 크기가 `rem = pt ÷ 9` 환산 범위 안인가?
- [ ] 모든 주장에 숫자가 붙어 있는가?
- [ ] [AUTHORING.md](AUTHORING.md)의 절대 규칙 6가지를 지켰는가?
